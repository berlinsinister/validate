import { ConfigDisplayI } from "@/types"

export const configDisplay: ConfigDisplayI = {
  options: [
    { component: 'fullName', label: 'Full Name' },
    { component: 'radio', label: 'Radio' },
    { component: 'dropdown', label: 'Dropdown' },
  ],
}
